console.log('JS LOADED')

// so what does this code need to do
// we need to be able to place the xs and os

const gameboard = { 1: '' }

const gridSpaces = document.querySelectorAll('.grid')

function PlaceTokens () {

}
